import { Routes } from '@angular/router';
import { UsersInterfaceComponent } from './users-interface/users-interface.component';

export const routes: Routes = [

    { path: 'users', component: UsersInterfaceComponent }

];
